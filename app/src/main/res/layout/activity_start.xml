<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_start"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/retro_race_track"
    tools:context="de.querra.mobile.runlazydroid.activities.StartActivity">

    <TextView
        android:layout_above="@+id/activity_start__loading"
        android:layout_marginBottom="80dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="Run Lazy Droid!"
        android:gravity="center"
        android:textSize="46sp"
        android:textColor="@color/white"/>

    <ProgressBar
        android:id="@+id/activity_start__loading"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:indeterminate="true"
        android:layout_centerInParent="true"/>

    <TextView
        android:layout_below="@+id/activity_start__loading"
        android:layout_marginTop="60dp"
        android:layout_centerHorizontal="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/white"
        android:textSize="18sp"
        android:text="@string/loading_data"/>

</RelativeLayout>